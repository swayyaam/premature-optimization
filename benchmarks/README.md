# benchmarks

Raw output from each load test, one file per run, named `NN-<step-name>.txt` to match the
step in `/docs`.

Nothing here is edited or rounded. The summaries live in the step documents and link back
to these files.

Empty until the load testing tool is set up in step 4.
